const expect = chai.expect

it('multiply', function () {
    expect(new NotificationToast("mocha")).to.have.property("setMessage")
})
//
// it ('fetch', function() {
//     expect(new FilterInputs()).to.have.
// })
